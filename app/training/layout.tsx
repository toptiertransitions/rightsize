export const dynamic = "force-dynamic";

export const metadata = { title: "Compliance Training — Top Tier Transitions" };

export default function TrainingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
